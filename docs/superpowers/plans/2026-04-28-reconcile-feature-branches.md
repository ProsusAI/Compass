# Cross-Branch Pipeline Reconciliation — Remaining Work

## Status (2026-04-28)

The cross-branch generalization is **6 of 7 increments complete**. Increments 1–6 — unifying `Candidate`, `SearchState`+`RoundSummary`, the Stage 4 MCP tool surface, `ReviewBriefing`, pipeline guards, and Review Agent prompts (three-tier base + overlays) — are committed on `feat/generalize-pipeline` (published) and produce the canonical shared scaffolding. The original Increment 7 ("per-strategy residuals") was scoped as three small per-branch ports.

A 2026-04-28 audit of the actual feature branches changed the picture:

| Branch | Commits since baseline `bb7b258` | Files | Lines | G | S | D | R |
|---|---|---|---|---|---|---|---|
| `feature/parallel-beam-search` | ~181 (incl. shared ancestry) | 128 | +21k/-3k | 47 | 47 | 25 | 7 |
| `feature/sms-emoa` | 203 | 162 | +24k/-4k | 56 | 12 | 55 | 80 |
| `feature/emosa` | 187 | 122 | +23k/-3k | 80 | 9 (+6 AMOSA-baseline) | 64 | 28 |

G = general-purpose improvement (lands on main). S = strategy-specific. D = defunct, subsumed by Increments 1–6 — drop. R = cross-cutting, manual review per commit.

A pure strategy-only port would discard months of G work. The revised plan is **Option 2 — reconcile-then-port**, split across multiple sessions.

---

## Cross-branch shared topics (the bulk of G)

Most G work appears on multiple branches with parallel evolution. Pick one canonical version per topic; ship one PR each.

| Topic | parallel-beam (rep. SHAs) | sms-emoa (rep. SHAs) | emosa (rep. SHAs) | Recommended canonical | Approx LOC |
|---|---|---|---|---|---|
| Search-tree visualization (round slider, oracle bounds, route labels, refresh protocol, dark mode) | `9081815`, `f584f10` | `a7224c7`, `df4dc12`, `83ff12a`, `ae45589`, `e753e87`, `2fbd2fd`, `9081815` | `df4dc12`, `a7224c7`, `7282e6c`, `35008bb`, `e79b0c6` | sms-emoa or emosa (most evolved) | 800–2,200 |
| Batch eval / concurrent evaluation / progressive screening / eval_status filtering | (yes) | `aae3550`, `27f8852`, `e433896`, `919741f`, `f0184c2`, `5c94e0c`, `2b8c9ef`, `3bc0941` | `3dde351`, `aae3550`, `f1627c2`, `f106645`, `74ba14b`, `919741f`, `ee8d238`, `169c1b7` | sms-emoa | 600–1,500 |
| Oracle metrics + ceiling capture + scatter bounds | `7379488` | `7379488`, `210bdf5`, `8642f88`, `3f681a0`, `38d0009` | `3f681a0`, `8642f88`, `593b9b2`, `6a9e38e`, `df5b6c5`, `d3520ca`, `31210c6` | emosa | 300–700 |
| Confusion analysis + contrast pairs + signal-to-noise filters + cell-exhaustion tracking | (yes) | `d21e3d0`, `daeb969`, `17415fa`, `a569d54`, `7608cac`, `adf651c`, `0e27514`, `cbbeba4` | `d21e3d0`, `daeb969`, `adf651c`, `7608cac`, `17415fa`, `cbbeba4` | sms-emoa or emosa | 700–1,200 |
| Route validation / canonical fields / wildcard / severity escalation (~~normalization~~ dropped 2026-04-30) | `4e4579d` | `433aaee`, `a1d1fd9`, `0338ffa`, `ecddefc`, `4e4579d` | `433aaee`, `4e4579d`, `b8018c1`, `a1d1fd9`, `279187a`, `599e1aa` | sms-emoa (`ecddefc`, `0338ffa`, subset of `a1d1fd9`); `433aaee` + `4e4579d` intentionally not ported (see A5) | ~155 (was 250–500) |
| Project-dir cache fix + metrics consolidation + run-specific prompt directories | `d354cdd`, `089ff41` | `d354cdd`, `6a9e38e`, `df5b6c5`, `1182b89`, `4f5c891`, `089ff41` | `1182b89`, `4f5c891`, `cdab117`, `24b42db`, `089ff41` | sms-emoa | 200–400 |
| Tooling: `query_misrouted_examples`, pagination, MCP cleanup | (yes) | `cdab117`, `2df60ae`, `764811b`, `856a85a` | (yes) | parallel-beam | ~500 |
| Cold-start prompt tightening / shallow-hypothesis rejection / target-driven scaffold | `8895b78`, `7cefacf`, `d41f2fd` | `eb10d13`, `fec025a`, `7c1c286`, `8895b78`, `7cefacf` | `2b8c9ef`, `3bc0941`, `2eaed8a`, `fde2c85`, `e3b3aa7`, `d5416f3`, `3f7fe50`, `d41f2fd`, `531d2d1`, `300b11f`, `51b3ca3`, `b07589e`, `1a9d11f`, `18b0a92`, `856a85a`, `4682749`, `210bdf5`, `a24d339` | emosa (most evolved) | 1,000–1,800 |
| Pipeline guards / dispatch markers / recovery phase | `e1da9b8`, `abcee7d`, `e9ae68d` | `91dfdbc`, `49f0bb7`, `4d58894`, `fe730bc`, `be2fd58` | `a52fa8e`, `08d4fda`, `d9ea800`, `27f8852`, `859010a`, `bd79c9e` | most are **D** (Increment 5 covers); small residual TBD | small |
| Review tool surface + child variants + secondary parent + preference resolution | (overlap) | `3c8cfb5`, `02019cd`, `f6a9712`, `41a3b98`, `fd69dca`, `41238fd`, `6f37cce` | `f54b92b`, `336433c`, `33ddf0e`, `febb931`, `41238fd`, `d1b9ba2`, `1f06f95` | mostly **D** (Increments 1, 4 cover); small residual | small |
| Documentation sync (architecture, search-strategy.md, narrative docs, design specs) | `2936fbb` | `e07eb3e`, `2936fbb`, `b8d9fc3`, `d121b73`, `106a9d2` | (yes) | merge across branches | ~500 |

## Per-branch unique G work

- **parallel-beam unique**: none after 2026-04-30 reclassification — `5e8fc0e` and the associated post-coldstart prompt + tests moved to **C1** as beam-specific.
- **sms-emoa unique**: none after 2026-04-30 reclassification — warmup test fixtures + scenarios moved to **C2** (sms-emoa-coupled state shape and tool names).
- **emosa unique**: small preprocessor pieces that bleed into S (handle in Phase C). ~0 sessions.

## Strategy-only residuals (Phase C)

After Phases A and B land, each feature branch's residual collapses to:

**parallel-beam** (~1 session)
- Files: `odysseus/agents/prompt_builder/search.py` (Pareto + crowding + HV + `prune_to_size` + `compute_pareto_front` + `update_elite_set`), `search_ops.py` (`advance_round` body with epsilon tightening, stagnation, cold-start floor)
- `algorithm_state` pocket: `beam_width`, `hypervolume`, `reference_point`, `epsilon_min`, `backtrack_threshold`
- Dispatcher: `_advance_beam` arm in `advance_step_tool`
- Preprocessor: populate `beam_rank`, `crowding_distance`, `hypervolume`, `reference_point`, and `stagnation_signal = {"hypervolume_delta", "backtrack_threshold"}`

**sms-emoa** (~1 session)
- Files: `odysseus/agents/prompt_builder/search.py` (`fast_non_dominated_sort`, `dynamic_reference_point`, `exclusive_hypervolume_contribution`, `reduce_population`), `search_ops.py` (`reduce_iteration`, `advance_warmup_batch`, termination)
- `algorithm_state` pocket: `mu`, `population`, `hypervolume_history`, `iteration`, `warm_up_complete`, `evaluation_budget`, `evaluations_used`, `reference_delta`, `stagnation_window`, `reference_point`
- Dispatcher: `_advance_sms_emoa` arm with two sub-arms (warmup vs steady-state); recognises `loop_phase ∈ {"warmup_seed", "warmup_build", "warmup_reduce", "review", "build"}`
- Preprocessor: populate `parent_a_version`, `parent_b_version`, and `stagnation_signal = {"hypervolume_history", "stagnation_window"}`
- SHAs to study for the algorithm port: `72eaf6e`, `a55f91d`, `e07eb3e`, `647e4ae`, `3c8cfb5`, `6aa6c69`, `022b811`, `91dfdbc`

**emosa** (~1.5 sessions)
- Files: `odysseus/agents/prompt_builder/annealing.py` (TrajectoryState, AnnealingState, `compute_tchebycheff_energy`, `compute_weight_vectors`, `normalize_objectives`, `metropolis_accept`, `compute_neighborhood`, `update_archive`); `search_ops.py` `advance_round` body with calibration handler
- Three EMOSA-specific commits to keep (the rest is AMOSA-baseline subsumed):
  - `c19ed6a` EMOSA refactor (Tchebycheff from ideal/nadir, plain archive, neighborhood replacement)
  - `9129f1f` ASF against per-trajectory reference points
  - `a15b608` recompute trajectory `current_energy` on ideal/nadir drift; `B = 4`
- `algorithm_state` pocket: `temperature`, `t_initial`, `t_min`, `alpha`, `num_trajectories`, `children_per_trajectory`, `trajectories: list[TrajectoryState]`, `ideal_point`, `nadir_point`, `neighborhood_size`, `phase`, `step_count`, `total_evals`, `max_evals`, `convergence_limit`, `epsilon`
- Dispatcher: `_advance_emosa` arm; calibration phase handler; **override `review_fanout_status`** with K-way per-trajectory fanout (the existing single-slot default in `odysseus/agents/pipeline/dispatch.py:review_fanout_status` is the override point)
- Preprocessor: populate `trajectory_id`, `weight_vector`, `binding_axis` (computed `argmax_i (λ_i · norm_i)`), `acceptance_history`, `stagnation_signal = {"temperature", "t_min", "review_exit"}`

## Defunct (drop, do not port)

Anything subsumed by Increments 1–6: `secondary_parent_version` on Candidate, `elite_set` rename, `RoundSummary` field renames, `ReviewBriefing` optional fields, build/review dispatch markers, three-tier review prompts, `advance_round_tool` → `advance_step_tool` rename. Specific D SHAs are catalogued in the per-branch triage reports (parallel-beam: 25, sms-emoa: 55, emosa: 64).

Reclassified D after A2 audit (2026-04-29):

- `2b8c9ef` (DirectiveBatch model): superseded by `ChildVariant` (`odysseus/agents/review/models.py:293–315`); the parallel-cold-start mechanism it introduced now lives in the A8 cold-start prompt at `odysseus/agents/prompts/review_agent_cold_start_base_system.md:33`, which already emits K `ChildVariant`s. Nothing left to port.
- `3bc0941` (`cold_start_beam_width=max`): adds a generic `SearchState.beam_width` default and threads it through `init_search_state`. `beam_width` is strategy-specific under the Increment 2 algorithm-state-pocket design and would land (if anywhere) in the beam overlay during Phase C1, not as generic G work.

## Cross-cutting / R commits (manual decision per commit)

Not enumerable in advance. Triage them at the start of the relevant session — most resolve to G or S after a 1-minute read.

---

## Progress tracker (~15 sessions, 30–45h spread across whatever cadence suits)

Tick each session as you complete it. The first session must be **S0** (commit this plan to the branch); after that, Phase A sessions are independent and can run in any order, while Phase B → C → D must be sequential.

When a session completes, update its row with: `[x]` instead of `[ ]`, the commit SHA(s) it produced on `feat/generalize-pipeline`, and a one-line note if anything deviates from the plan. Keep the plan file in sync on the branch (amend the relevant tracking commit or push a fresh "progress: ..." commit).

### Phase 0 — Setup

- [ ] **S0** — Commit this plan to `feat/generalize-pipeline` at `docs/superpowers/plans/2026-04-28-reconcile-feature-branches.md`. Use the Haiku-subagent commit workflow. *(must run before any other session)*

### Phase A — Cross-branch G to main (8 sessions, any order after S0)

- [x] **A1** — Search-tree visualization. Ported the canonical viz from `feature/sms-emoa` (`search_tree.py`, `viz.py`, `scripts/viz_search_tree.py`, `tests/test_search_tree.py`) — explicitly chose sms-emoa over emosa after a strategy-coupling audit (2 vs 62 strategy-specific term hits in the respective `search_tree.py`s; emosa's "trajectory coloring / temperature lines / reheat markers" are exactly the coupling we want to avoid on the generalized branch). Genericization applied during the port (not after): (Cat A) module docstring rewritten to be strategy-neutral; rename local `population` → `elite_entries` and flip lookup precedence to `elite_set` first / `population` legacy fallback; remove the hardcoded `"mu": 8` default. (Cat B injection seams) added `_STRATEGY_LABELS` dict + `__STRATEGY_LABEL__` HTML placeholder substituted by `render_html` (drives the `<title>` and header `<span>`); added `_algorithm_chips(state_data)` adapter emitting `DATA.algorithm_chips` — only the SMS-EMOA adapter registered now, beam/EMOSA adapters land in Phase C; HTML JS at L709 replaced with a chip loop so beam/EMOSA runs render with no SMS-EMOA-specific chip. **Wiring**: `_try_write_viz` called after `_save_state` / `_save_pending` in `init_search_state`, `register_candidate`, `record_eval_result`, and `advance_round` (best-effort — wrapped in try/except so viz failure can never break the loop). **Tests**: ported `tests/test_search_tree.py` with fixture migration (population/mu/hypervolume_history → elite_set + algorithm_state) plus one legacy-shape regression fixture preserved + new `test_strategy_seams_for_non_smsemoa` for the beam-no-chip path; new `tests/test_viz_wiring.py` smoke test exercises the `init_search_state` → `register_candidate` → `viz.html` flow end-to-end. **Deviations**: (1) `odysseus/util.py` doesn't exist on this branch — inlined the 5-line `load_json` helper byte-identical to the sms-emoa source rather than create a util module out of scope; (2) `prompt_builder/README.md` didn't exist — created from scratch mirroring the sms-emoa README's table style, scoped to modules present on this branch; (3) `pyproject.toml` gained a `per-file-ignores` entry suppressing E501 only for `search_tree.py` because the embedded HTML/CSS/JS template literal cannot be wrapped without breaking rendering — the 3 non-template lint issues (I001, UP035, SIM108) were fixed properly. Out-of-scope as noted in the plan: live-replay refresh protocol beyond `_try_write_viz`, route labels, per-strategy overlays beyond `mu` (defer to Phase C). End-to-end render verified against synthetic states for both sms_emoa (chip emitted, title "SMS-EMOA — Run …") and parallel_beam (no chip, title "Parallel Beam Search — Run …"); all three template placeholders (`__STRATEGY_LABEL__`, `__RUN_ID__`, `__DATA__`) substitute cleanly. Verification: `1161 passed, 1 failed` (only pre-existing `test_example_config_round_trip`); ruff 5 / pyright 37 (both at pre-existing baselines).
- [x] **A2** — Batch eval + concurrent evaluation + eval_status lifecycle + recovery. Ported the canonical end state from sms-emoa (`aae3550` minus screening, `27f8852`, `919741f`, partial `74ba14b` filtering rule, `e433896` lint) over four commits: `d63b86f` (1/4 — `SearchState.active_evals` field, `Candidate.eval_status` lifecycle in `register_candidate` / `record_eval_result`, `advance_round` filters `complete` from elite update + active_evals guard + all-failed stagnation path; `eval_status=None` from legacy state files treated as `complete` for backward compat); `2246985` (2/4 — new `odysseus/eval/batch_eval.py` with normal-mode concurrent dispatch via `asyncio.gather` over per-candidate `_run_single_eval`, auto-transition `loop_phase → "review"` + `clear_build_dispatched` on drain; **rate-limiter audit found `controller.run` was instantiating its own `TokenBucketRateLimiter`** so a one-line canonical fix was ported — added optional `RunDependencies.rate_limiter`, controller uses injected when present, `run_batch_eval_impl` shares one limiter across N candidates); `d13ae0d` (3/4 — `run_batch_eval` MCP tool with lazy import to break a cycle with `prompt_building_tools.build_pipeline_config`; `_BUILD_TOOLS` updated; `_detect_stage_4_phase` returns `"build_recovering"` when `active_evals` non-empty; `STAGE_4_BUILD_RECOVERING_INSTRUCTION` ported from `919741f` with eval_status string remap and tool-list narrowing; `is_build_recovering` helper in `pipeline/dispatch.py`); `92b6859` (4/4 — recovery branch in `run_batch_eval_impl`: when called with `candidates=[]`, reads `pending.json`, smart-skips candidates whose `report.json` is already on disk via `_try_load_existing_report`, re-runs the rest; `_finalize_drain` helper now shared between normal mode and recovery). **Skipped per design lock**: progressive screening (canonical added then rolled back via `aae3550`/`5c94e0c`); `f0184c2` generic survivors (`dev_ratio` 0.2→0.1; `stagnation_limit` 3→2 / `convergence_limit` 5→4 / `epsilon` 0.001→0.003 SearchState defaults; `directive_history` cap to last 15) plus the manual MCP smoke test of the recovery flow (A2-followup dropped per 2026-04-30 user direction). (The `cold_start_beam_width: int = 3` field from `f0184c2` is *not* in the followup; it is strategy-specific and belongs in the beam overlay if anywhere — Phase C1.) `2b8c9ef` (DirectiveBatch model) and `3bc0941` (cold_start_beam_width=max) were originally listed here but were reclassified to Defunct after the 2026-04-29 audit (see the Defunct section above); `f1627c2` (beam diversity validation, → Phase C); `ee8d238`, `169c1b7` (D — subsumed by Increments 1–6); the beam_width / mutation_strategy / source_directive_batch_id fields on `BatchEvalCandidate` (out-of-scope strategy bits from `74ba14b`). **Report path convention**: `<output_dir>/<run_id>/eval/<prompt_version>/report.json`. End-to-end recovery flow: mid-build crash → restart → `_detect_stage_4_phase` returns `"build_recovering"` → orchestrator dispatches recovery sub-agent → sub-agent calls `run_batch_eval(run_id, candidates=[])` → recovery completes in-flight evals (smart-skipping any that already finished) → drains `active_evals` → flips to `"review"`. Verification across the four commits: `1132 → 1146 → 1149 → 1151 passed, 1 deselected` (only pre-existing `test_example_config_round_trip`); ruff held at the 5-error baseline (an unused-import drift was fixed in passing during commit 2); pyright held at the 37-error baseline. Manual MCP smoke test dropped along with A2-followup (2026-04-30 user direction).
- [x] **A3** — Oracle metrics + ceiling capture. **2026-04-29 audit found most of the topic surface already on-branch**: `OracleMetrics` model + `compute_oracle_metrics{,_from_report}` + per-target oracle ceiling / capture-ratio translation / `target_above_oracle` were all in baseline; `_extract_quality_score` consumer-side fallback to `oracle_quality_captured` (batch_eval.py:180,208) landed in A2; `compute_cost_quality_change` always-emit oracle keys came in A6 (`6a9e38e`); search-tree scatter bounds (delta + absolute, oracle ceiling) came in A1; `31210c6` cleanup (`load_directive_batches`) was already gone; `593b9b2`/`6a9e38e`/`df5b6c5` ported in A7-followup/A6/A7. Residual was just two tightly coupled fixes shipped over two commits: `d47428a` (1/2 — port `3f681a0`: replaced `compute_oracle_metrics` ratio `Δq / Δq_oracle` with `(1 + Δq) / (1 + Δq_oracle)` so `candidate_quality_captured` stays in `[0, 1]` for mixed-sign deltas; executive-summary wording "X% captured" → "X% of oracle"; updated `test_normal_case` assertion); `945cd63` (2/2 — port `8642f88`: `compute_cost_quality_change` now extracts `quality_change`/`oracle_quality_change` as locals and emits `oracle_quality_captured` using the same ratio formula, so the consumer fallback in batch_eval is no longer dead code; added `TestOracleQualityCaptured` (4 cases — normal, mixed-sign in `[0,1]`, zero oracle, empty results) and `TestExtractQualityScorePreference` (5 cases) — 9 new tests). **Reclassified after audit**: `210bdf5` and `38d0009` (oracle infrastructure / decoupling) subsumed by A6's always-compute + the existing OracleMetrics model; `7379488` (scatter bounds viz, sms-emoa/parallel-beam SHA — not on emosa) subsumed by A1; `d3520ca` (archive + beam-coupled viz) deferred to **Phase C1** (beam-specific). Three-tier prompt scan clean (no metric-semantics text needed updating). Verification across both commits: `524 passed → 533 passed, 1 expected failure` (only pre-existing `test_example_config_round_trip`); ruff held at the 5-error baseline; pyright held at the 37-error baseline.
- [x] **A4a** — Confusion analysis foundations: data models, preprocessor, briefing wiring. Ported `d21e3d0` + `daeb969` as `ec2a7fe` (add `ConfusionImpact`, `ContrastPairContent`, extend `EditDirective.block_type` literal with `contrast_pair`, add `ReviewBriefing.confusion_analysis`); `17415fa` as `299c31d` with **dedup-by-`example_id` extension** so `build_confusion_analysis` accepts results from N candidates without inflating per-cell counts (param renamed `best_candidate_results` → `eval_results`); `a569d54` + `7608cac` as `2bb6d09` with **strategy-owned selector lift**: `_select_confusion_candidates(state)` defaults to `[c.prompt_version for c in state.elite_set]` — no primary-metric fallback, strategies own confusion candidates via the elite_set contract (Increment 2). Briefing now carries populated `confusion_analysis` end-to-end; executive summary surfaces top 3 cells with structural/mixed/prompt-sensitive labels. Review Agent prompt unchanged in A4a (lands in A4b). **Decided A7-followup coupling: option (a)** — per-target `source_version` + `single_candidate_meets_all` flag. Confusion analysis stays decoupled from per-target source_version (different concerns: confusion uses elite_set; target_progress uses per-metric leaders). Verification: `1091 passed, 1 deselected` (`test_example_config_round_trip` pre-existing); ruff 5 / pyright 37 (both pre-existing baselines).
- [x] **A4b** — Confusion analysis Phase 2: signal-to-noise filters (`4e280cf`), prompt updates adapted to three-tier structure (`adf651c` + `279187a`), cell-exhaustion tracking (`0e27514` — adds 5 fields to `ConfusionImpact`, `ChildVariant.target_confusion_cell`, `cell_attempt_history.json` persistence, diversity allocation rule). Also: redirect `odysseus/mcp/resources.py:114` to a tier file and delete legacy `review_agent_system.md` (currently still referenced). Heads-up: the existing `0e27514` source has a latent bug where `last_attempted_round` is always `None` because `update_cell_attempt_history` doesn't write `"round"` into entries — fix during port. Cross-link with A7-followup (option (a) is the chosen design). **Ported SHAs**: `4e280cf` (signal-to-noise), `adf651c` (mostly superseded by three-tier rewrite), `279187a` (prompt builder fix), `0e27514` (cell-exhaustion tracking). **Bug fix landed**: `update_cell_attempt_history` now accepts `current_round` and writes `"round": current_round` in every entry — `last_attempted_round` is now correctly populated. Regression test added to `tests/test_cell_attempt_history.py`. **Scoped-down prompt port** (4 edits, not wholesale): documented 5 new `ConfusionImpact` fields + `effective_impact` sort + `attempt_count >= 2` switch-fix-type rule in `review_agent_iterative_base_system.md`; added `target_confusion_cell` to ChildVariant output schema and `contrast_pair_content` schema in `review_agent_base_system.md`; applied `279187a` verbatim to `prompt_builder_system.md`. Explicit diversity-allocation paragraph and anti-pattern #5 intentionally **dropped** — superseded by the base prompt's distinctness self-check. **MCP resource redirect**: `review_agent_guidelines` now returns `review_agent_base_system.md` + `"\n\n---\n\n"` + `review_agent_iterative_base_system.md` (algorithm-neutral). Legacy `review_agent_system.md` deleted. `docs/architecture.md` row 275 updated. **A7-followup decoupling preserved**: cell-exhaustion uses `state.elite_set` via A4a's `_select_confusion_candidates`; independent of `target_progress`'s per-metric leaders. **Verification**: `1120 passed, 1 failed` (pre-existing `test_example_config_round_trip`); ruff 5 / pyright 37 (both at pre-existing baselines).
- [x] **A5** — Route validation + canonical fields + wildcard + severity escalation. Ported `ecddefc` + `0338ffa` as `a02db03` (1/2 — flip `route_in_routes` `SchemaFinding` `severity="warning"` → `"critical"` so a mismatch between `expected.route` and `expected.routes` keys blocks pipeline advancement instead of silently zeroing cost/quality metrics; align `data_validation_system.md` decision-rule list so the agent doesn't downgrade the finding back to a flag; docstring tweak in `tests/test_models.py`). Ported a **subset of `a1d1fd9`** as `8239559` (2/2 — `canonical_route_fields` schema-conformance check flagging route objects with field names other than `{"cost", "quality_score"}`; wildcard path support in `data_validation/transform.py` via new `_get_nested_wildcard` / `_set_nested_wildcard` and a dispatcher branch so mappings like `tier_details.*.score → expected.routes.*.quality_score` work; 11 new wildcard tests + 4 new `TestCanonicalRouteFields` tests). **Dropped per 2026-04-30 user direction**: `433aaee` (the 6-tier `normalize_route` module + backend label injection + auto-injected `route_validation` metric) — the LLM-output drift problem was solved by extracting canonical labels from the dev/holdout split, so a runtime normalization layer is not wanted on this branch; `4e4579d` (`route_key` property on `Expected` + cascade rename `expected.route` → `expected.route_key`) — `route_key` exists only to wrap `normalize_route` and has no purpose without it. **Dropped from `a1d1fd9` as out-of-A5-scope**: the `MetricDeltas` → `dict[str, float]` review-pipeline refactor (`review/models.py`, `review/preprocessor.py`, `tests/test_review_models.py`, `tests/test_review_preprocessor.py`) — orthogonal to route validation; the branch is intentionally in a mixed state (the new `BatchOutcome.metric_deltas_vs_parent: dict[str, float]` already landed via A7 while `CandidateAnalysis.delta_vs_parent: MetricDeltas` is still old-style); finishing the refactor is a separate task if desired. Also skipped: `prompts/review_agent_system.md` (file deleted in A4b — N/A) and the `agents/__init__.py` / `agents/review/__init__.py` empty-init shuffles. Verification across both commits: full suite holds at 1144 passed (only `tests/test_models.py::test_example_config_round_trip` deselected/expected-fail — pre-existing); ruff held at the 5-error baseline; pyright held at the 37-error baseline. Targeted: `tests/test_data_validation_checks.py` 50 passed (4 new `TestCanonicalRouteFields` + 46 pre-existing); `tests/test_data_ingestion_transform.py` 37 passed (11 new wildcard tests + 26 pre-existing).
- [x] **A6** — Project-dir cache + metrics consolidation + run-specific paths. Ported `6a9e38e` (always compute accuracy/confusion/f1/cost_quality_change) and `d354cdd` (test fixture: reset `_cached`; align metric assertions). Reclassified `df5b6c5` and `1182b89` to A7 (review-tool surface, not project-dir/metrics/prompts) and deferred `4f5c891` to A7 (`get_prompt_text_tool` doesn't yet exist on this branch — comes with `1182b89`). Skipped `089ff41` (pure util consolidation; `odysseus/util.py` not present and not blocking). Result: `test_returns_cwd` flipped fail → pass; only `test_example_config_round_trip` remains pre-existing.
- [x] **A7** — Tooling + briefing scaffolding. Ported `1182b89` as `c543f7a` (replace mutation-centric schema with child-variant / batch-outcome model; add `query_holdout_examples_tool` + `get_prompt_text_tool`; merge `TargetSlack` into `UserTargetProgress`); `df5b6c5` as `bf53a03` (decompose `record_directive_outcomes_tool` params; allow null `parent_preference`); `4f5c891` + `cdab117` as `2ce1487` (run-specific prompt-dir fallback; require `run_id` on `get_prompt_text_tool`); `764811b` + `2df60ae` as `93aff90` (offset pagination; persistence-threshold contrast-pair text was already absent on this branch, so no prompt edit was needed; also fixed a B009 ruff regression introduced by `c543f7a`). **Deferred `856a85a`** (best-per-target metrics for `target_progress`): the source commit ships no `mixed_candidates` flag or per-metric `source_version`, and the Review Agent reads `target_progress` to emit `LoopSignal` consumed by `search_ops.py:378` for termination — silently mixing metrics across candidates can cause early stop on imaginary success. Tracked as new row A7-followup below. Verification: `1067 passed, 1 deselected` (only pre-existing `test_example_config_round_trip`); ruff back to baseline 5 errors; pyright unchanged at 37 pre-existing.
- [x] **A7-followup** — Best-per-target metrics for `target_progress` (deferred from A7 / source `856a85a`). **Design fixed by A4a: option (a)** — add per-target `source_version` and a `single_candidate_meets_all` flag, plus prompt updates so the Review Agent can distinguish single-prompt success from cherry-picked mixes. (Option (b) was considered and rejected: keeping globally-best semantics with persistence/cell-exhaustion biases the agent toward declaring rounds done, raising the risk of early-exit on cherry-picked metrics.) Decoupled from confusion analysis: A4a's `_select_confusion_candidates` already uses `state.elite_set` (strategy-owned), independent of per-target leaders. **Ported `593b9b2`** (not `856a85a`): replaced primary-metric `max` with holistic tuple `(targets_met_count, sum_capped_progress, min_progress)`; added `source_version` to `UserTargetProgress`, `single_candidate_meets_all` to `ReviewBriefing`; updated `review_agent_iterative_base_system.md` and `review_agent_base_system.md` with loop-exit rule; synced `docs/architecture.md`. Three private helpers added (`_count_targets_met`, `_sum_capped_progress`, `_min_progress`); 4 new integration tests. No deviation from plan. Commit SHA: d24be34
- [x] **A8** — Cold-start prompt tightening + target-driven scaffold. Scoped down per session direction: ported only the **cognitive-strategy axis** from emosa's "Dimensions of Variation" (`531d2d1`) — added as a fifth bullet inside step 2 of `review_agent_cold_start_base_system.md` and threaded through the diversity-criterion paragraph so the distinctness test covers it. Replaced the trailing one-line "self-check, per the base" with a locally-scoped self-check (parallel-beam `8895b78`): explicit **grounding test**, **reconstruction test**, and **distinctness test** that fire against the K hypothesis strings before the base self-check. Iterative base: added a single `priority_weight` row to the `target_progress` field table — `priority_weight` was computed (`preprocessor.py:316–324`) and surfaced in the executive summary already, but undefined in the prompt; the new row teaches its meaning + a usage rule (bias K-child allocation toward higher-weight deficits first), without breaking the single-target-per-variant flow. **Scoped out**: the other three Dimensions axes (source of difficulty, coverage allocation, reasoning depth — already implicitly covered by the existing per-route-rationale / boundary / source-of-hardness / fix bullets); `b07589e`'s aggressiveness paragraph + cross-target trade-off worked example (incompatible with the iterative step-1 "Pick **one** specific target" rule and would invite multi-mechanism hypotheses the self-check is designed to reject); beam-specific cold-start SHAs `3bc0941`, `3f7fe50` (strategy-specific, belong in beam overlay if anywhere). **Preprocessor wiring verified pre-existing from A7**: `compute_target_slack` already merged into `UserTargetProgress` via `build_review_briefing` (`preprocessor.py:1063`), and `build_review_briefing` is the single entry point for both cold-start and iterative briefings — no preprocessor change was required. Strategy-term audit clean (no `trajectory`, `weight_vector`, `lambda`, `Tchebycheff`, `binding_axis`, `mutation_mode`, `parent_a/b`, `beam_rank`, `crowding_distance`, `hill-climb`, `beam`, `sms-emoa`, `emosa` in either edited base file). Verification: `1126 passed, 1 failed` (only pre-existing `test_example_config_round_trip`); `tests/test_review_agent_prompt_assembly.py` 93/93 pass; ruff/pyright unchanged at pre-existing baselines. Commit SHA: 378acb9

### Phase B — Per-branch G residuals (sequential after Phase A)

- [~] **B1** — Reclassified 2026-04-30. `5e8fc0e` (cold-start elite floor) and `a5473ea`'s `review_agent_post_coldstart_system.md` are beam-specific (the elite-floor mechanism only matters when round 1 produces K > 1 cold-start variants, and it's coupled to the post-coldstart review prompt that overrides parent selection to "exactly one child per protected parent" — neither half is useful alone). Folded into **C1**. No other generic infra unique to `feature/parallel-beam-search` was identified after Phase A. **Drop.**
- [~] **B2** — Reclassified 2026-04-30. The four sms-emoa "warmup fixture" artefacts (`tests/scenarios/16_sms_emoa_warmup_then_iterations.md`, `tests/scenarios/17_stage4_build_dispatch_guard.md`, `tests/scenarios/data/sms_emoa_toy_dataset.jsonl`, and the `tests/scenarios/data/review/generate_fixtures.py` rewrite) hardcode sms-emoa-only top-level state fields (`mu`, `population`, `warm_up_complete`, `hypervolume_history`, `iteration`) and call sms-emoa-only tools (`advance_warmup_batch_tool`, `reduce_iteration_tool`). The generalized branch carries those inside the `algorithm_state` pocket (`odysseus/agents/prompt_builder/search.py:181`); the fixtures fail to load against the canonical schema and the scenarios can't execute without the `_advance_sms_emoa` dispatcher arm. Folded into **C2**. emosa-unique was already ~0 sessions per the per-branch table. **Drop.**

### Phase C — Strategy-only ports (sequential per branch, after Phase B)

- [x] **C1** — parallel-beam residual port. Cut `feat/generalize-beam` from `feat/generalize-pipeline` (Phase A not yet merged to `main`; per-user direction 2026-04-30 to branch off the integration branch instead of waiting). Landed in 4 commits + 1 strategy-neutral prep commit on the parent branch:
  - **Prep (on `feat/generalize-pipeline`, SHA `f591258`)**: `docs(review): add per-parent cell diversity rule to iterative base`. Anti-pattern guardrail in step 3 of the iterative review workflow — when a dispatch produces multiple children sharing a `parent_version`, no two siblings may target the same `target_confusion_cell`. Two children with different parents *may* target the same cell. Strategy-neutral; lands on the parent branch so C2/C3/C4 inherit it. The post-coldstart override therefore does not need to mention cell allocation (one child per parent ⇒ per-parent uniqueness is trivially satisfied).
  - **Commit 1/4 (`6a2b409`)**: beam algorithm primitives in `odysseus/agents/prompt_builder/search.py` — `compute_pareto_front` (refactored to reuse existing `dominates`), `crowding_distance`, `compute_hypervolume`, `find_knee_point`, `prune_to_size`, `update_elite_set(..., is_cold_start_round=False)`, `validate_elite_set`. Module-level `logger` added. Existing `update_pareto_front` left intact (still used by hill-climb). +33 unit tests (`TestParetoAlgorithms` × 18, `TestUpdateEliteSetPareto` × 6, `TestValidateEliteSet` × 4, `TestUpdateEliteSetColdStart` × 5).
  - **Commit 2/4 (`ba9947d`)**: `_advance_beam` dispatcher arm + cold-start floor. New `advance_round_beam` in `search_ops.py` reads/writes the algorithm-state pocket: `beam_width` (default 3), `epsilon_min` (0.0005), `backtrack_threshold` (2), `hypervolume` / `prev_hypervolume` (0.0), `reference_point` ((0.0, 0.0)), `targets_met_epsilon_tightened` (False). `state.epsilon` (top-level, default 0.001) is the stagnation threshold; `convergence_limit` / `stagnation_limit` / `max_rounds` stay top-level. Round-1 path: `update_elite_set(is_cold_start_round=True)` retains all scored variants regardless of dominance; Pareto validation skipped on round 1; stagnation increments only from round 2. Hypervolume-based stagnation against `state.epsilon` with one-time epsilon tightening when all user targets first met. `RoundSummary.backtracking: bool = False` added (parallel to existing strategy-specific optional fields like `hypervolume`, `acceptance_rates`). 4 private helpers added: `_load_user_targets`, `_load_candidate_metrics`, `_reshape_route_metrics`, `_check_all_targets_met`. Round reports persisted via existing `review.ops.save_round_report`. **Source-branch archive logic intentionally not ported** — canonical uses pending+state without an archive concept. Dispatcher updated: `algorithm == "beam"` → `_advance_beam`; existing `_advance_hill_climb` left untouched. `tests/test_search_ops_advance_round.py` ported from `5e8fc0e` (5 integration tests across `TestAdvanceRoundColdStartFloor` + `TestAdvanceRoundNormalParetoResumesInRound2`). Existing `test_non_hill_climb_raises_not_implemented` updated to use `sms_emoa` as the unimplemented fallback.
  - **Commit 3/4 (`3ba8022`)**: post-coldstart Review tier. New algorithm-neutral prompt `review_agent_post_coldstart_base_system.md` (override layer that inherits the iterative diagnostic workflow and overrides only parent-selection / merge sections for round 2). `assemble_review_prompt(algorithm, "post_coldstart")` composes a four-tier prompt: shared base + iterative phase base + post-coldstart override + iterative strategy overlay (overlays reused — no new per-strategy prompts). MCP prompt `odysseus_review_agent_post_coldstart(algorithm)` registered + added to `_REVIEW_AGENT_PROMPT_NAMES` so `orchestrator_tools` dispatches via the assembler. `_detect_stage_4_phase` returns the **derived** phase `"review_post_coldstart"` when `loop_phase == "review"` AND `round == 1` AND `algorithm == "beam"` (gated to beam — hill-climb runs at round 1 don't have protected parents). Raw on-disk `loop_phase` stays `"review"`; `_VALID_LOOP_PHASES` deliberately *not* extended (deviation from the C1 implementation plan, validated against source SHA `5e8fc0e`). `STAGE_4_REVIEW_POST_COLDSTART_INSTRUCTION` and `phase_config["review_post_coldstart"]` added. `orchestrator_tools` phase-derivation extended with the `post_coldstart` branch. **Also fixes a latent `.gitignore` bug**: unanchored entries `prompts/`, `backends/`, `data/`, `outputs/`, `configs/`, `scratch/` matched nested source dirs (e.g. `odysseus/agents/prompts/`) — anchored each with a leading `/`. Mirrors source-branch fix `a5473ea`. Tests: +11 in `test_review_agent_prompt_assembly.py` (4-tier compose + overlay map), +7 in `test_pipeline_status.py` (algorithm/round/loop_phase gating + `_next_action_for_stage_4` entry).
  - **Commit 4/4 (`cde6ab8`)**: preprocessor enrichment + viz adapter + scenario + algorithm-id reconciliation. New `_populate_beam_review_fields(search_state, elite_set)` in `preprocessor.py` pulls `hypervolume` / `prev_hypervolume` / `backtrack_threshold` / `reference_point` from the pocket, computes `beam_rank` (every Pareto-front member is rank 0) + `crowding_distance` (with a JSON-friendly finite sentinel for inf via the existing `search.crowding_distance` utility), emits `stagnation_signal = {"hypervolume_delta", "backtrack_threshold"}`. `build_review_briefing` dispatches on `state.algorithm == "beam"` and splats `beam_overrides` into the `ReviewBriefing` constructor; non-beam runs collapse to `None` for beam-only fields and keep the hill-climb-shaped `stagnation_signal`. `search_tree._STRATEGY_LABELS` keyed under `"beam"` (was `"parallel_beam"` from A1 — A1's id was inconsistent with the canonical `AlgorithmType` literal); display label `"Parallel Beam Search"` preserved. `search_tree._algorithm_chips` gains a beam adapter emitting `beam_width`, `hypervolume` (rounded), `epsilon`. `tests/scenarios/16_round1_cold_start_protection.md` ported from `feature/parallel-beam-search` with branch ref dropped, `odysseus_review_agent` → `odysseus_review_agent_iterative` (canonical name), `algorithm: "parallel_beam"` → `"beam"`. `tests/scenarios/README.md` row added. `docs/architecture.md` rounded out: `"review_post_coldstart"` row, `ReviewBriefing` strategy-specific field documentation. +4 unit tests across `test_review_preprocessor.py` and `test_search_tree.py`.
  - **Verification at C1 tip (`cde6ab8`)**: `1249 passed, 2 deselected` (only pre-existing `test_example_config_round_trip` and `test_returns_cwd`). Pyright 37 (pre-existing baseline). Ruff 8 (pre-existing — note: my own per-commit verification reported 5 in commits 1–3 but the actual baseline at C3 tip was 8; the three extra are `F541` in `test_pipeline_status.py:462`, `I001` + `F401` in `test_routing_context.py:3`, none introduced by C1).
  - **Source-branch state at port time**: `feature/parallel-beam-search:f1be332`. The canonical algorithm id is `"beam"` (matches `odysseus/agents/prompt_builder/search.py:23` `AlgorithmType` literal). C2/C3/C4 should also use the canonical id and not introduce `"parallel_beam"` again.
- [ ] **C2** — sms-emoa residual port. Branch `feat/generalize-sms-emoa` off `feat/generalize-pipeline` (same approach as C1 — Phase A still not merged to `main`). Add `_advance_sms_emoa` (warmup + steady-state), `reduce_population` etc., preprocessor population. Verify warmup → steady-state transitions; (μ+1) eviction sound.
  - **Test fixtures + scenarios (folded from B2)**: port `tests/scenarios/16_sms_emoa_warmup_then_iterations.md`, `tests/scenarios/17_stage4_build_dispatch_guard.md`, `tests/scenarios/data/sms_emoa_toy_dataset.jsonl`, and the `tests/scenarios/data/review/generate_fixtures.py` rewrite. Adapt fixture state shape to canonical (sms-emoa fields go inside `algorithm_state`, not top-level), and update scenario tool names if the dispatcher consolidates `advance_warmup_batch_tool` / `reduce_iteration_tool` behind `advance_step_tool`.
- [ ] **C3** — emosa residual port (first half). Branch `feat/generalize-emosa` off main. Land `annealing.py`, AnnealingState in pocket, `_advance_emosa` calibration arm, preprocessor `binding_axis`. Verify K=5 trajectories complete calibration round and write per-trajectory state.
- [ ] **C4** — emosa residual port (second half). Per-trajectory `review_fanout_status` override; `trajectory_fanout_missing`; multi-child Metropolis acceptance; neighborhood replacement. Verify K-way review fanout, per-trajectory acceptance, scenario runs to convergence.

### Phase D — Cross-branch verification (final)

- [x] **D** — Diff audit. Commit `89b23d2`. **Diff base correction**: ran `git diff feat/generalize-pipeline..<branch> -- odysseus/` (not `main..<branch>`) — Phase A is not yet on `main`, so the integration branch is the correct base per the C1 design lock. Audit results — all three strategy branches collapse to the predicted shape from the "Strategy-only residuals (Phase C)" section (no out-of-shape files):
  - **beam @ `cde6ab8`** (12 files, +788/-25): `prompt_builder/search.py` (`compute_pareto_front` / `crowding_distance` / `compute_hypervolume` / `find_knee_point` / `prune_to_size` / `update_elite_set` / `validate_elite_set`) + `search_ops.py:advance_round_beam` + `_advance_beam` dispatcher + `_populate_beam_review_fields` preprocessor + `review_agent_post_coldstart_base_system.md` + `review_post_coldstart` derived phase + viz chip in `search_tree.py`. Two tiny non-code edits (`prompt_builder_system.md` doc note flip, `server.py` `_REVIEW_AGENT_PROMPT_NAMES` += `odysseus_review_agent_post_coldstart`) — both within scope.
  - **sms-emoa @ `ce30c45`** (8 files, +938/-17): `prompt_builder/search.py` (`fast_non_dominated_sort` / `dynamic_reference_point` / `exclusive_hypervolume_contribution` / `reduce_population`) + `search_ops.py` (`reduce_iteration` / `advance_warmup_batch`) + `_advance_sms_emoa` dispatcher with two sub-arms + warmup phase detection in `pipeline/status.py` + warmup phase instructions + preprocessor enrichment + viz chip update.
  - **emosa @ `92f390f`** (11 files, +1684/-29): `prompt_builder/annealing.py` new module (TrajectoryState, AnnealingState, `normalize_objectives`, `compute_tchebycheff_energy`, `compute_asf_energy`, `metropolis_accept`, `compute_weight_vectors`, `compute_cooling_rate`, `update_archive`, `compute_neighborhood`, `replace_if_better`) + `search_ops.py` calibration + steady-state advance + `_advance_emosa` dispatcher + **`review_fanout_status` override** in `pipeline/dispatch.py` (added `algorithm` kwarg; delegates to `trajectory_fanout_missing` for `algorithm="emosa"`) + per-trajectory child variants + dispatch tracking in `review/ops.py` + preprocessor enrichment + viz chip.

  **`docs/architecture.md` updated**: new "## 6. Cross-Branch Strategy Matrix" section added between MCP Surface (§5) and Directory Guide (§7); old §6/§7 renumbered to §7/§8. The L220 fanout note updated to point at the override seam (`review/ops.py:trajectory_fanout_missing` + `pipeline/dispatch.py` `algorithm="emosa"` arm) and cross-link to §6.

  **Legacy-branch decision (locked)**: keep `feature/parallel-beam-search`, `feature/sms-emoa`, `feature/emosa` intact per user direction 2026-04-30 — needed for side-by-side testing against the generalized branches. No deletion, no force-push, no rebase.

  Verification: `1181 passed, 1 deselected` (only pre-existing `test_example_config_round_trip` deselected); ruff held at the 5-error baseline; pyright held at the 37-error baseline (Phase D is docs-only on `feat/generalize-pipeline`).

### Completed prior to this plan revision (for context)

- [x] **Increment 1** — Unify `Candidate`. Commit `8e55ed8`.
- [x] **Increment 2** — Unify `SearchState` + `RoundSummary`. Commit `398c5e3`.
- [x] **Increment 3** — Unify Stage 4 MCP tool surface (`advance_round_tool` → `advance_step_tool`). Commit `fe62325`.
- [x] **Increment 4** — Unify `ReviewBriefing`. Commit `a4a9bfc`.
- [x] **Increment 5** — Unify pipeline guards & dispatch markers. Commit `9425121`.
- [x] **Increment 6** — Review Agent prompts (three-tier base + 8 overlays). Commit `925e2e6`.

## Per-session verification

Each session ends with:
```
uv run ruff check odysseus/ tests/
uv run pyright
uv run pytest tests/ -x
```

The two pre-existing failures (`tests/test_models.py::test_example_config_round_trip`, `tests/test_project_dir.py::TestGetProjectDir::test_returns_cwd`) remain pre-existing throughout. Phase C ports also run an MCP scenario test that exercises the strategy end-to-end on a small dev set. Phase D runs the cross-branch diff audit.
