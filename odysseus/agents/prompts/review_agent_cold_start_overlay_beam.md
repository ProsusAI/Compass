Extends `review_agent_cold_start_base_system.md`.
**Loop phase.** `review`, round == 1.
**K.** `beam_width`.
**Parent selection.** initial compiled prompt. Diversity must span confusion cells *and* cost regions — the beam uses crowding distance in later rounds.
